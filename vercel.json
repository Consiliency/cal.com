{
  "buildCommand": "NODE_OPTIONS='--max-old-space-size=8192' yarn build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "corepack enable && corepack prepare yarn@3.8.7 --activate && rm -f .yarnrc.yml && echo 'nodeLinker: node-modules' > .yarnrc.yml && yarn install",
  "framework": "nextjs",
  "regions": ["iad1"]
}
